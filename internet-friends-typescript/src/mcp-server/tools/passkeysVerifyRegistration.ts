/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passkeysVerifyRegistration } from "../../funcs/passkeysVerifyRegistration.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.PasskeyVerificationRequest$inboundSchema,
};

export const tool$passkeysVerifyRegistration: ToolDefinition<typeof args> = {
  name: "passkeys-verify-registration",
  description: `Verify passkey registration response

Verifies a WebAuthn registration response from the client`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passkeysVerifyRegistration(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
